{
  "title": "Skool Paid Communities Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "maxItems": {
      "title": "Max Communities",
      "type": "integer",
      "description": "Maximum number of paid communities to scrape. Leave empty for no limit.",
      "default": 100
    },
    "maxScrolls": {
      "title": "Max Scrolls",
      "type": "integer",
      "description": "Maximum number of times to scroll down the discovery page to load more communities.",
      "default": 20
    }
  },
  "required": []
}
